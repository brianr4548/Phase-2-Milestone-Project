

<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand font-weight-bold">Cities</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link" href="#"><%= link_to "Home", sessions_new_path %></a>
      <a class="nav-item nav-link active"><%= link_to "User Page", user_path(session[:id])%></a>
      <a class="nav-item nav-link" href="#"><%= link_to "Restaurants", restaurants_path %></a>
    </div>
  </div>
</nav>



<%= simple_form_for :search, url: cities_path, method: "GET", html: { class: 'form-inline' } do |f| %>
  <%= f.input :name, input_html: {value: ""} %>   
  <%= f.submit "Search", class: "btn btn-primary" %>
<% end %>







<div class="container mt-4">
<div class="row d-flex justify-content-between">

<%@cities.each do |city|%>
<div class="card" style="width: 18rem;">
  <div class="card-body">
  <h3 class= "text-dark"><%=link_to city.name, city_path(city)%>
    <h5 class="card-text restaurants"><%= city.state%></h5>
  </div>
</div>
<%end%>
  </div>
</div>




<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand font-weight-bold">Restaurants</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav w-100">
        <div class="container-fluid d-flex w-100 justify-content-between">
          <div class="d-flex flex-row">
            <%= link_to "Home", sessions_new_path, class:"nav-item nav-link" %>
            <%= link_to "User Page", user_path(session[:id]),class:"nav-item nav-link" %>
            <%= link_to "All Cities", cities_path, class:"nav-item nav-link" %>
          </div>
          <div>
            <%= link_to "Log out", logout_path, method: :delete, class:"nav-item nav-link text-right"%>
          </div>
      </div>
    </div>
  </div>
</nav>

<br>


<main class="container p-0">
  <div class="bg-light p-0 rounded">
    <h1>Check out our favorite restaurants.</h1>
    <p class="lead">These will soon be your favorites, too!</p>
  </div>
</main>

<main class="container p-0">
<div class="bg-light p-0 rounded">
<%= simple_form_for :search, url: restaurants_path, method: "GET", html: { class: 'form-inline' } do |f| %>
  <%= f.input :name, input_html: {value: ""} %>   
  <%= f.submit "Search", class: "btn btn-dark" %>
<% end %>
</div>
</main>

<br>


<div class="container mt-4">
<div class="row d-flex justify-content-between">

<%@restaurants.each do |restaurant|%>
<div class="card" style="width: 18rem;">
  <div class="card-body">
  <h3 class= "text-dark"><%=link_to restaurant.name, restaurant_path(restaurant)%>
    <h5 class="card-text restaurants"><%= restaurant.description%></h5>
  </div>
</div>
<%end%>
  </div>
</div>






